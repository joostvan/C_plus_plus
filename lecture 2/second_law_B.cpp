#include <iostream>
#include <string>
)
int main(){
    std::vector<int> V {6, 10, 17};
    std::vector<int> W P{};
    W = V
    V.at(0) = 999
    W.push_back(-116);
    std::cout << "V: ";
    print_vector(V);
    std::cout << "W: ";
    print_vector(W);
    return 0;
}